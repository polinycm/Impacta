@model EPT.Models.Profissionais

@{
    ViewData["Title"] = "Excluir";
}

<div class="col-1 mb-4">
    <nav class="navbar navbar-expand-lg">
        <button type="button" id="sidebarCollapse" class="btn btn-info">
            <i class="fas fa-align-left"></i>
        </button>
    </nav>
</div>

<div class="alterar">
    <h3>Confirmar exclusão deste funcionario ?</h3>
</div>
<div class="alterar">
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            ID
@*             @Html.DisplayNameFor(model => model.ProfissionaisId)
 *@        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ProfissionaisId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>
    <br />
    <div class="row">
        <div >

            <form asp-action="Excluir">
                <input asp-route-idprofissional="@Model.ProfissionaisId" type="submit" value="Confirmar" class="btn btn-success" /> |
                <a href="@Url.Action("Index")" class="btn btn-danger">Voltar</a>
            </form>
        </div>
       

    </div>
</div>
